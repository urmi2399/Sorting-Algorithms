* We have used Python 3.1.0
* Only the dependency required is python interpreter and numpy installation.
* You can either run on python's IDLE or IntelliJ.
* Open any of the sort algorithm and run them, 4 files with different array length will be generated if not present already.
* This random array would be used to perform sorting functions.
* Create data files is used to generate content, read and write in those text files

